services:
  media:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
      - ./media:/media:ro
      - ./cache:/cache
    devices:
      - /dev/dri:/dev/dri
    tmpfs:
      - /tmp:size=2G
    restart: unless-stopped
