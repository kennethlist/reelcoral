services:
  media:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yml:/config/config.yml:ro
      - ./media:/media:ro
      - ./cache:/cache
    devices:
      - /dev/dri:/dev/dri
    tmpfs:
      - /tmp:size=2G
    restart: unless-stopped
